name = "dashboard-holasuite"
main = "src/worker.js"
compatibility_date = "2026-04-02"

# Serve the whole repo as static assets via `env.ASSETS.fetch()`
assets = { directory = ".", binding = "ASSETS" }

